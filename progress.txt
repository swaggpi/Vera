VERA — PROGRESS LOG
Rebuild context each iteration: `git log --oneline -50` + this file + feature-requirements.md

== CURRENT STATE (2026-07-25) ==
P0 foundation + P1 briefing vertical slice COMPLETE and verified on the build box (Tier-1):
  - `./gradlew test` passes (core unit tests: RSS/Atom parse, catalog, briefing gen + fallback,
    streak/XP, spaced repetition, diet meter).
  - `./gradlew assembleDebug` builds app-debug.apk (~18 MB).
App runs against FakeLlmEngine (deterministic) — no Gemma model needed to build/test.

== VERIFIED ==
  bash run-tests.sh   # green: unit tests + debug APK

== NOT YET VERIFIED (needs device — Tier-2, Pixel 7a / GrapheneOS) ==
  - Real Gemma inference (MediaPipeLlmEngine is a documented stub; DI binds FakeLlmEngine)
  - Any Compose UI / instrumented behavior
  - Live RSS fetching (SampleData is the offline fallback)

== NEXT TASKS (pick ONE, implement end-to-end, test, commit) ==
  1. Add `SpeechService` + `SearchProvider` interfaces + fakes in :core (unblocks P3).
  2. WorkManager: schedule morning/evening briefings + notification.
  3. Wire MediaPipeLlmEngine + model side-load flow (device verify).
  4. Port SIFT coach + inoculation games from ../vera-prototype.html into :feature training.
  5. GitHub Actions CI running run-tests.sh.

== NOTES ==
  - Toolchain: JDK 21, Android SDK at $HOME/android-sdk (init.sh installs it), Gradle wrapper 8.9.
  - Everything device/model/network-bound sits behind an interface + fake → JVM tests stay the gate.
  - Tagesschau feed is non-commercial, 60 req/hr — keep source-agnostic for any public release.
